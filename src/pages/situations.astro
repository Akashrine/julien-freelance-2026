---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import SituationsIntro from '../components/SituationsIntro.astro';
import SituationsObservables from '../components/SituationsObservables.astro';
import SituationsAnalysis from '../components/SituationsAnalysis.astro';
import SituationsIntervention from '../components/SituationsIntervention.astro';
import Footer from '../components/Footer.astro';
import { getWebPageSchema, getBreadcrumbSchema, BreadcrumbItems } from '../utils/schema';

const siteUrl = 'https://julien-brionne.fr';
const pageUrl = `${siteUrl}/situations`;
const webPageSchema = getWebPageSchema(
  pageUrl,
  'Vos situations',
  'Reconnaître le moment critique. Quand l\'effort augmente plus vite que l\'impact et que personne n\'ose plus trancher. Situations où j\'interviens pour fermer les options, trancher les arbitrages, assumer les décisions impopulaires.',
  ['situations d\'intervention', 'saturation système décisionnel', 'trancher', 'arbitrage produit']
);
const breadcrumbSchema = getBreadcrumbSchema([
  BreadcrumbItems.home,
  BreadcrumbItems.situations,
]);
---

<Layout
  title="Situations — Julien Brionne"
  description="Reconnaître le moment critique. Quand l'effort augmente plus vite que l'impact et que personne n'ose plus trancher. Situations où j'interviens pour fermer les options, trancher les arbitrages, assumer les décisions impopulaires."
  ogTitle="Reconnaître le moment critique"
  ogDescription="Quand l'effort augmente plus vite que l'impact et que personne n'ose plus trancher. Fermer les options, trancher les arbitrages."
  ogType="website"
  ogImage="/og/page-situations.webp"
  ogUrl="https://julien-brionne.fr/situations"
  twitterCard="summary_large_image"
  twitterTitle="Reconnaître le moment critique"
  twitterDescription="Quand l'effort augmente plus vite que l'impact et que personne n'ose plus trancher. Fermer les options, trancher les arbitrages."
  webPageSchema={webPageSchema}
  breadcrumbSchema={breadcrumbSchema}
>
  <Navbar currentPage="situations" />
  <main>
    <SituationsIntro />
    <SituationsObservables />
    <SituationsAnalysis />
    <SituationsIntervention />
    <Footer />
  </main>
</Layout>
