---
interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---

<div class={`max-w-2xl mx-auto reveal ${className}`}>
  <div class="bg-[#FAFAF9] border border-gray-200/60 p-10 relative organic-shape">
    <slot />
  </div>
</div>
